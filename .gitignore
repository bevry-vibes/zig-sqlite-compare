# macOS Finder metadata
.DS_Store

# zig build caches and outputs (regenerated by `./scripts/bootstrap.sh` or `zig build`)
.zig-cache/
zig-out/
bin/

# zig dependency package cache (regenerated by `./scripts/bootstrap.sh` or `zig build`)
zig-pkg/